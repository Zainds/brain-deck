# BrainDeck

Приложение для интервального повторения (Spaced Repetition), подготовки к экзаменам.

## Функциональность

- 📚 Создание и управление колодами карт
- 📝 Добавление и редактирование карточек
- 🧠 Режим обучения с алгоритмом интервального повторения (SM2)
- 📊 Статистика обучения
- 🔐 Аутентификация пользователей

## Предварительные требования

- Node.js (версии 18 или выше)
- npm (обычно устанавливается вместе с Node.js)

## Установка и запуск

1. **Клонируйте репозиторий:**

   ```bash
   git clone <repository-url>
   cd brain-deck
   ```

2. **Установите зависимости:**

   ```bash
   npm install
   ```

3. **Настройка переменных окружения:**

   Создайте файл `.env` в корне проекта и добавьте ключи вашего проекта Supabase:

   ```env
   VITE_SUPABASE_URL=your_supabase_project_url
   VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
   ```

4. **Настройка базы данных:**

   Примените миграции из папки `supabase/migrations` к вашему проекту Supabase, чтобы создать необходимые таблицы.

5. **Запуск в режиме разработки:**

   ```bash
   npm run dev
   ```

   Приложение будет доступно по адресу `http://localhost:5173`.

